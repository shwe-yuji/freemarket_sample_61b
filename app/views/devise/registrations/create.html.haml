- @p_bar = 5
.wrapper
  = render 'devise/shared/header', p_bar: @p_bar

  .container
    %h2.title 会員登録完了
    .form
      .form__fields
        .end
          ありがとうございます。
          %br
            会員登録が完了しました
        .submit
          .submit__btn
            = link_to "marpacriをはじめる", "/"
  .banner
    -# 仮置のバナーです。現在トップページにあるバナーと置き換え予定
    %p
      = image_tag 'logo_white.svg', width: '340px', class: 'img'
  = render 'devise/shared/footer'
